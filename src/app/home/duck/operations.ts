import {Creators} from './actions';

const moveCard = Creators.moveCard;

const createCard = Creators.createCard;

const createBoard = Creators.createBoard;

const changeBoard = Creators.changeBoard;

export default {
    moveCard,
    createCard,
    createBoard,
    changeBoard,
};
